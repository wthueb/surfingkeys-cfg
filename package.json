{
    "name": "surfingkeys-cfg",
    "module": "src/index.ts",
    "type": "module",
    "scripts": {
        "build": "tsc -noEmit && esbuild src/index.ts --outfile=surfingkeys.js",
        "upload": "npm run build && gh gist edit $(gh gist list | grep surfingkeys.js | awk '{ print $1 }') surfingkeys.js"
    },
    "devDependencies": {
        "esbuild": "^0.19.5",
        "prettier": "^3.0.3",
        "typescript": "^5.2.2"
    }
}

{
    "name": "surfingkeys-cfg",
    "module": "src/index.ts",
    "type": "module",
    "scripts": {
        "build": "bun build src/index.ts --outfile=surfingkeys.js",
        "upload": "npm run build && gh gist edit $(gh gist list | grep surfingkeys.js | awk '{ print $1 }') surfingkeys.js"
    },
    "devDependencies": {
        "bun-types": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
